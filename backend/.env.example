# Porta exigida pelo enunciado
PORT=9000

# Banco (os defaults batem com o docker-compose)
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USER=nutri
DATABASE_PASSWORD=nutri
DATABASE_NAME=nutri

# LLM — sem chave, a API cai no gerador determinístico (regras clínicas)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
